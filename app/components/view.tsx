import React from "react";
import Profile from '../components/view/profile'

// Incase we want to swap the entire viewset we are using later.

const ViewPage = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default ViewPage;